env:
  Database__ConnectionString: "Host=denji-postgresql;Port=5432;Username=denji;Password=denji;Database=denji;"
  IdentityUI__BaseUrl: "https://power-ui.local"
  Cors__Origins__0: "https://cosmo.local"

ingress:
  enabled: true
  className: ""
  annotations:
    kubernetes.io/ingress.class: nginx
    cert-manager.io/cluster-issuer: selfsigned-issuer 
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: denji.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls:
    - secretName: denji-local-tls
      hosts:
        - denji.local

postgresql:
  enabled: true
  global:
    postgresql:
      auth:
        username: "denji"
        password: "denji"
        database: "denji"